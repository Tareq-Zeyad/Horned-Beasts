import React from 'react';
import Form from 'react-bootstrap';

class Form extends React.component{
    render () {
        return(
            <>

            </>
        )
    }
}

export default Form;